#include <iostream>
#include <algorithm>
#include <vector>
#define endl '\n'
#define io ios::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr)
using namespace std;
#define INF 100000005
vector<string> vec;



int main() {

    int N; unsigned long M = 0; cin >> N;

    while(N--){
        string x; cin >> x;
        M += x.size();
    }

    for(int i = 0 ; i < )





}